export const add = (a, b) => a + b;

export const substract = (a, b) => a - b;

export const multiply = (a, b) => a * b;

export const divide = (a, b) => a / b;

// This module handles the main arithmetic logics to be used for the operations.